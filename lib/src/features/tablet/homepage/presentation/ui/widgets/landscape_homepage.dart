

import 'package:flutter/material.dart';

class TabLandscapeHomepage extends StatelessWidget {
  const TabLandscapeHomepage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('LANDSCAPE')),
    );
  }
}
