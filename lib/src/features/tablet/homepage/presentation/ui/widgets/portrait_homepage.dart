

import 'package:flutter/material.dart';

class TabPortraitHomepage extends StatelessWidget {
  const TabPortraitHomepage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('PORTRAIT')),
    );
  }
}
