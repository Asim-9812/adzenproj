import 'package:flutter/material.dart';

class TabPotraitDashboard extends StatelessWidget {
  const TabPotraitDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('TAB POTRAIT'),
      ),
    );
  }
}
