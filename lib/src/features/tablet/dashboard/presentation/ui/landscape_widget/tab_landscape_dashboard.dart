import 'package:flutter/material.dart';

class TabLandscapeDashboard extends StatelessWidget {
  const TabLandscapeDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('TAB LANDSCAPE'),
      ),
    );
  }
}
